<article class="card mb-3">

    <div class="card-body">
        <p>{{item.origin}}</p>
        <p>{{item.shortURL}}</p>
        <a href="#" class="btn btn-danger">Eliminar</a>
        <a href="#" class="btn btn-warning">Editar</a>

    </div>
</article>